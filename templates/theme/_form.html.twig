{% import 'form/form_macros.html.twig' as form_macros %}

{{ form_macros.form_start(form) }}

{% set general_content %}
    {{ form_row(form.name) }}
{% endset %}
{{ form_macros.form_card('Informations', general_content) }}

{{ form_macros.buttons(button_label|default('Enregistrer'), 'save', 'app_theme_index') }}

{{ form_macros.form_end(form) }}
