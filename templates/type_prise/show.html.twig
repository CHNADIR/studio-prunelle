{% extends 'base.html.twig' %}

{% block title %}TypePrise{% endblock %}

{% block body %}
    <h1>TypePrise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_prise.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_prise.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_prise_index') }}">back to list</a>

    <a href="{{ path('app_type_prise_edit', {'id': type_prise.id}) }}">edit</a>

    {{ include('type_prise/_delete_form.html.twig') }}
{% endblock %}
