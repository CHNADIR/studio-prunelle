{% extends 'base.html.twig' %}

{% block title %}TypeVente{% endblock %}

{% block body %}
    <h1>TypeVente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_vente.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_vente.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_vente_index') }}">back to list</a>

    <a href="{{ path('app_type_vente_edit', {'id': type_vente.id}) }}">edit</a>

    {{ include('type_vente/_delete_form.html.twig') }}
{% endblock %}
